<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
						http://www.springframework.org/schema/beans/spring-beans.xsd">
	<!-- xml을 로딩할때 자동으로 객체가 생성된다. -->
	<!-- id속성이 없다면 객체의 주소값을 받아서 사용할수 없다(다른방법이 존재(자동주입기능이용) -->
	<bean class='kr.co.softcampus.beans.TestBean' />

	<!-- xml을 로딩할때 자동으로 객체가 생성된다. -->
	<!-- id속성에 이름을 부여하면 getBean 매서드를 통해 객체의주소값을 받아 사용할수 있다  -->
	<!-- 생성된 객체는 더이상 생성되지 않는다. Singleton -->
	<bean id='test1' class='kr.co.softcampus.beans.TestBean'/>
	
	<!-- lazy-init="true" xml을 로딩 할때 객체가 생성 되지 않는다 (생략하면 false)-->
	<!-- getBean 매소드를 호출할때 객체가 생성되며 singleton 이기때문에 객체는 하나만 생성된다 -->
	<bean id='test2' class='kr.co.softcampus.beans.TestBean' lazy-init="true"/>
	
	<!-- scop : prototype xml을 로딩할때 객체가 생성도지 않는다. -->
	<!-- getBean 매소드를 호출할때 마다 새로운 객체를 생성해서 변환한다 -->
	<bean id='test3' class='kr.co.softcampus.beans.TestBean' scope='prototype'/>

</beans> 